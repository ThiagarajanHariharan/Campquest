## 2026-04-24 - ARIA labels for Icon-only Buttons
**Learning:** React icon-only buttons often lack accessibility context, causing screen readers to ignore them or announce them generically.
**Action:** Always ensure icon-only buttons (like `✕`, `🚪`, `☰`, `⚙️`) include descriptive `aria-label`s so visually impaired users can interpret them properly.
